<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="ScrcpyGui.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ScrcpyGui"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Scrcpy GUI (WinUI 3)"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="Scrcpy GUI (WinUI 3)">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/Square44x44Logo.scale-200.png" />
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView x:Name="NavView" 
                        Grid.Row="1"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="True"
                        SelectionChanged="NavView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="DeviceItem" Content="Devices" Icon="CellPhone" Tag="Devices" />
                <NavigationViewItem x:Name="DownloadItem" Content="Package Manager" Icon="Download" Tag="PackageManager" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" Margin="12" />
        </NavigationView>
    </Grid>
</Window>
